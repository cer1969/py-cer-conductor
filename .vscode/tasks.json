{
    "version": "0.1.0",
    "command": "python.exe",
    "isShellCommand": true,
    "args": ["-u", "${file}"],
    "showOutput": "always",
        "options": {
        "cwd": "${fileDirname}"
    }
}